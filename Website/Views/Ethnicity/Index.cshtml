@model IEnumerable<Website.Models.Ethnicity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EthnicityId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EthnicityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(model => item.EthnicityId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EthnicityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EthnicityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EthnicityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EthnicityId })
        </td>
    </tr>
}

</table>
